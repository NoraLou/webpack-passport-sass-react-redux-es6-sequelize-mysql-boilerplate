/* this is a reducer */

const initialState = {
};

export default function appReducer(state = initialState, action) {
  switch (action.type) {
    /* add some case statements here */

    default:
      return state;
  }
}
